# AI Tools Website

A simple static website scaffold for an AI tools homepage.

## Files

- `index.html` — landing page structure
- `styles.css` — responsive styling
- `script.js` — dynamic tool card rendering and button interaction

## Usage

Open `index.html` in a browser to preview the site.
